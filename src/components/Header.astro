---
const pathname = Astro.url.pathname;
const links = [
  { href: '/insights', label: 'Insights' },
  { href: '/case-studies', label: 'Case studies' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' },
];
const isActive = (href: string) =>
  pathname === href || pathname === href + '/' || pathname.startsWith(href + '/');
---

<header class="sticky top-0 z-50 backdrop-blur-md bg-base/80 border-b border-divider/60">
  <nav class="mx-auto max-w-[1100px] px-6 h-16 flex items-center justify-between">
    <a
      href="/"
      class="font-semibold tracking-tight text-fg hover:text-accent transition-colors"
    >
      signal-to-noise<span class="text-accent">.</span>co
    </a>
    <ul class="flex items-center gap-4 sm:gap-8 text-sm">
      {links.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class:list={[
              'transition-colors',
              isActive(href) ? 'text-accent' : 'text-muted hover:text-fg',
            ]}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>
